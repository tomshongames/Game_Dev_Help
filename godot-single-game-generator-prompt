Treat this as an automatic Godot code generation task, not a teaching task.

I will give you a request inside square brackets:
[GAME OR FEATURE DESCRIPTION]

Your job is to generate exactly one complete Godot script that implements that request as safely, clearly, and practically as possible.

Main priority:
- Correct behavior and safe integration come first.
- Generate code directly, not a review.
- The script must be ready to paste into a Godot project.
- If something cannot be safely assumed, make it configurable with exported variables.
- Never pretend unseen scenes, nodes, resources, settings, inputs, groups, animations, buses, autoloads, or files already exist.

Rules:
- Output exactly one full code block.
- Do not output multiple scripts.
- Do not output explanations, alternatives, file structures, scene trees, or extra notes unless absolutely necessary for safety.
- Keep the script self-contained unless I explicitly provide project context.
- Use the safest node access style for unknown projects:
  - prefer exported references for nodes or resources
  - use direct paths only if clearly justified
- Do not invent project setup.

Godot rules:
- Match the Godot version and syntax style visible in my request.
- Do not mix Godot 3 and Godot 4 syntax.
- Use snake_case for variables, functions, parameters, and signals.
- Use PascalCase for class names when needed.
- Use UPPER_CASE for constants.
- Do not invent InputMap actions unless necessary. If input is needed and no action name is provided, use a clearly configurable placeholder or configurable key handling.

Quality rules:
Prioritize:
1. correctness
2. safe integration
3. clarity
4. maintainability
5. consistency
6. safe efficiency

Optimization rules:
- Only apply safe optimizations.
- Do not change gameplay timing, physics, signal timing, input behavior, animation flow, loading behavior, or other sensitive behavior unless explicitly requested.

When the request is incomplete:
- Only implement what is supported by the description.
- Keep missing parts configurable.
- Do not fake completeness.

Output format:
- Return exactly one Godot code block and nothing else.

DESCRIPTION TO IMPLEMENT:
[GAME OR FEATURE DESCRIPTION]
